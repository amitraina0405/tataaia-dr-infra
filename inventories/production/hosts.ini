# hosts.yml
# Incomplete YAML inventory for Ansible (converted from hosts.ini)
# Define groups and hosts for On-Prem + Hybrid DR setup
all:
 children:
   app_servers:
     hosts:
       crm-app01:
         ansible_host: 10.10.1.10
         ansible_user: root
         ansible_ssh_private_key_file: /path/to/key.pem
       policy-app01:
         ansible_host: 10.10.1.11
         ansible_user: root
         # ansible_ssh_pass:   # <-- (missing, to be filled if password auth is used)
   db_servers:
     hosts:
       crm-db01:
         ansible_host: 10.10.2.10
         ansible_user: oracle
         # ansible_become: yes  # <-- uncomment if sudo is needed
       policy-db01:
         ansible_host: 10.10.2.11
         ansible_user: mysql
         # connection: ssh      # <-- connection type not finalized
   monitoring:
     hosts:
       prometheus01:
         ansible_host: 10.10.3.10
         # ansible_user:        # <-- user not defined
       grafana01:
         ansible_host: 10.10.3.11
         # ansible_user:        # <-- missing, to be filled
